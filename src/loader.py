from testDW import Checking, File, Directory, Contact, Search, Commands
from const import DIR_PATH, lstCmd, lstVal

check = Checking(lstVal)
file = File(DIR_PATH)
direct = Directory(DIR_PATH)
cont = Contact(DIR_PATH)
search = Search(DIR_PATH)
comm = Commands(lstCmd)

